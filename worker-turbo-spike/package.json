{
  "name": "worker-turbo-spike",
  "private": true,
  "version": "0.0.0",
  "description": "THROWAWAY spike: can a Cloudflare Worker sign an ANS-104 data item with the funding key and upload it to Turbo (genuine Arweave)?",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev --local --port 8792",
    "size": "wrangler deploy --dry-run --outdir .bundle-check",
    "deploy": "wrangler deploy",
    "teardown": "wrangler delete"
  },
  "dependencies": {
    "@dha-team/arbundles": "^1.0.4",
    "bs58": "^5.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260721.1",
    "typescript": "7.0.2",
    "wrangler": "4.113.0"
  }
}
