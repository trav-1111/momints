{
  "name": "momints-roll-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Momints roll backend: Cloudflare Worker + D1 + Irys/Arweave. Single Worker (validated by spikes — see docs/spikes/irys/RESULT2.md).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "bundle-size": "wrangler deploy --dry-run --outdir .bundle-check",
    "db:migrate:local": "wrangler d1 migrations apply momints-rolls --local",
    "db:migrate:remote": "wrangler d1 migrations apply momints-rolls --remote"
  },
  "dependencies": {
    "@cf-wasm/photon": "^0.3.7",
    "@irys/upload": "0.0.15",
    "@irys/upload-solana": "0.1.8",
    "@metaplex-foundation/mpl-core": "^1.10.0",
    "@metaplex-foundation/umi": "^1.5.1",
    "@metaplex-foundation/umi-bundle-defaults": "^1.5.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260721.1",
    "typescript": "7.0.2",
    "wrangler": "4.113.0"
  }
}
